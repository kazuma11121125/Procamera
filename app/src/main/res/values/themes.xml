<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose renders all UI; this base theme only needs to exist for the
         Activity's window background before the first Compose frame. -->
    <style name="Theme.ProCamera" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
